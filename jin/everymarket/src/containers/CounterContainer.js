import React from "react";
import Counter from "../components/Counter";

const CounterContainer = () => {
  return <Counter />;
};

//이 컴포넌트를 Redux와 연동하기 위해서는 connect 함수를 사용

export default CounterContainer;